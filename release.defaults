%% NB: This file was generated by `configure'

%% to have the stuff in `var' and `etc' hosted outside of the release folder,
%% just set this to "/"
{root, ""},
{logdir,        "{{root}}var/log/ejabberd"},
{accesslog,     "{{logdir}}/access.log"}.

{webroot,       "{{root}}var/www"}.
{mnesia_dir,    "{{root}}var/data"}.
{inetrc_file,   "{{root}}etc/inetrc"}.
